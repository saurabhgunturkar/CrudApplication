#CrudApplication manadatory steps to run the CrudApplication

1. To run the angular project, call "ng serve" command.
2. To install json-server for calling the API data, run "npm install -g json-server" command.
3. To start json-server, use "json-server --watch db.json" command.

